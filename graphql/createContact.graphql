mutation createContact(
  $name: String!
  $email: String!
  $phone: String!
  $message: String!
) {
  createContact(
    input: {
      data: { name: $name, email: $email, phone: $phone, message: $message }
    }
  ) {
    contact {
      id
    }
  }
}
